[project]
name = "api-db-rollover"
version = "0.1.0"
description = "Migrates new data to public API table"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "curb-utils",
    "dotenv>=0.9.9",
]

[build-system]
requires = ["uv_build>=0.9.22,<0.10.0"]
build-backend = "uv_build"
