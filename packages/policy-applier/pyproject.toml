[project]
name = "policy-applier"
version = "0.1.0"
description = "Apply policies to curb segments"
readme = "README.md"
authors = []
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.8,<0.12.0"]
build-backend = "uv_build"
